name: Build and test

on:
  push:
  pull_request:

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
          - name: Windows
            os: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes cmake ninja-build qt6-base-dev libarchive-dev zlib1g-dev

      - name: Install Windows Qt
        if: runner.os == 'Windows'
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.3'
          target: 'desktop'
          arch: 'win64_msvc2022_64'

      - name: Install Windows native dependencies
        if: runner.os == 'Windows'
        shell: pwsh
        run: vcpkg install --triplet x64-windows

      - name: Restore Libretro database cache
        uses: actions/cache@v4
        with:
          path: build/src/libretro-database-1.22.1
          key: libretro-database-1.22.1

      - name: Provision Libretro database
        shell: bash
        run: |
          if [ ! -d build/src/libretro-database-1.22.1/rdb ]; then
            mkdir -p build/src
            curl --fail --location --retry 3 --output /tmp/libretro-database.tar.gz \
              https://github.com/libretro/libretro-database/archive/refs/tags/v1.22.1.tar.gz
            tar -xzf /tmp/libretro-database.tar.gz -C /tmp
            mv /tmp/libretro-database-1.22.1 build/src/libretro-database-1.22.1
          fi

      - name: Configure
        shell: bash
        run: |
          if [ "$RUNNER_OS" = "Windows" ]; then
            cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE="$VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake"
          else
            cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
          fi

      - name: Build
        run: cmake --build build --parallel

      - name: Test
        shell: bash
        env:
          QT_QPA_PLATFORM: offscreen
        run: ctest --test-dir build --output-on-failure
